global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'nestjs-backend'
    static_configs:
      - targets: ['host.docker.internal:3000'] # if running Nest locally; adjust host
    metrics_path: /metrics
